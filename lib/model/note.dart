import 'package:flutter/animation.dart';

class Note {
  String title;
  String content;
  Color bgColor;

  Note({
    required this.title,
    required this.content,
    required this.bgColor,
  });
}
